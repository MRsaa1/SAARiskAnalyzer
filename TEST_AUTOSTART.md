# 🧪 Тестирование автозапуска

## Как проверить, что автозапуск работает:

### 1️⃣ Остановите сервисы
```bash
./manage-services.sh stop
```

### 2️⃣ Проверьте, что они остановлены
```bash
./manage-services.sh status
lsof -i :8084 -i :3001
```

### 3️⃣ Перезагрузите компьютер
```bash
sudo reboot
```

### 4️⃣ После перезагрузки проверьте статус
Откройте терминал и выполните:
```bash
cd ~/saa-risk-analyzer
./manage-services.sh status
```

Ожидаемый результат:
- Backend должен работать на порту 8084
- Frontend должен работать на порту 3001

### 5️⃣ Проверьте доступность
```bash
# Backend API
curl http://localhost:8084/health

# Frontend в браузере
open http://localhost:3001
```

## 🔍 Если что-то не работает

### Проверьте логи
```bash
./manage-services.sh logs
```

### Проверьте, загружены ли сервисы
```bash
launchctl list | grep com.saa
```

Должны быть видны:
- `com.saa.backend`
- `com.saa.frontend`

### Перезапустите вручную
```bash
./manage-services.sh restart
```

## ✅ Быстрая проверка (без перезагрузки)

Вместо перезагрузки можно симулировать автозапуск:

```bash
# Остановите сервисы
./manage-services.sh stop

# Выгрузите из launchd
launchctl unload ~/Library/LaunchAgents/com.saa.backend.plist
launchctl unload ~/Library/LaunchAgents/com.saa.frontend.plist

# Загрузите снова (как при перезагрузке)
launchctl load ~/Library/LaunchAgents/com.saa.backend.plist
launchctl load ~/Library/LaunchAgents/com.saa.frontend.plist

# Проверьте статус
sleep 5
./manage-services.sh status
```

## 📝 Ожидаемое поведение

После правильной настройки:
- ✅ Сервисы запускаются при загрузке macOS
- ✅ Сервисы автоматически перезапускаются при падении
- ✅ Работают в фоновом режиме
- ✅ Логи сохраняются в `/tmp/`

Если все работает - автозапуск настроен правильно! 🎉



