# Отладка черного экрана

## Проблема
- Консоль браузера пустая
- Экран черный
- http://localhost:3001

## Возможные причины

1. **JavaScript не выполняется**
   - Проверьте консоль браузера (F12)
   - Проверьте вкладку Network - загружается ли `/src/main.tsx`?

2. **Ошибка в коде**
   - Откройте консоль браузера (F12)
   - Проверьте вкладку Console на наличие ошибок (красным)
   - Проверьте вкладку Network на ошибки загрузки файлов

3. **Проблема с CSS**
   - Страница может быть черной из-за темного фона
   - Проверьте, загружается ли CSS

## Что проверить

1. **Откройте консоль браузера (F12)**
   - Должны быть логи:
     - "🔥 main.tsx loaded"
     - "✅ Root element found"
     - "✅ React root created"
     - "✅ React rendered"
     - "🚀 App mounted"
     - "🚀 Dashboard mounted, loading portfolios..."

2. **Проверьте вкладку Network**
   - Загружается ли `/src/main.tsx`?
   - Загружается ли `/src/App.tsx`?
   - Загружается ли `/src/pages/Dashboard.tsx`?
   - Есть ли ошибки (красным)?

3. **Проверьте вкладку Elements**
   - Есть ли элемент `<div id="root">`?
   - Есть ли что-то внутри него?

## Если консоль пустая

1. **Проверьте, что frontend запущен:**
   ```bash
   ./manage-services.sh status
   ```

2. **Проверьте, что порт 3001 свободен:**
   ```bash
   lsof -ti:3001
   ```

3. **Проверьте логи frontend:**
   ```bash
   tail -50 /tmp/vite_clean.log
   ```

## Временное решение

Если проблема сохраняется, попробуйте:
1. Полностью закрыть браузер
2. Очистить кэш браузера
3. Открыть в режиме инкогнито
4. Проверить другой браузер

## Что должно быть видно

После исправлений вы должны увидеть в консоли:
- "🔥 main.tsx loaded"
- "✅ Root element found"
- "✅ React root created"
- "✅ React rendered"
- "🚀 App mounted"
- "🚀 Dashboard mounted, loading portfolios..."
- "🔄 Loading portfolios..."

Если этих логов нет, значит JavaScript не выполняется.

