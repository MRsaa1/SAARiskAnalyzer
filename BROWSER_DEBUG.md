# 🔍 Отладка Frontend

## Текущий статус:
- ✅ HTML загружается
- ✅ CSS загружается (тёмная тема работает)
- ✅ JavaScript загружается
- ❌ Черный экран без форм

## 🛠 ЧТО СДЕЛАТЬ:

### 1. Откройте Developer Tools в браузере:

**Safari:**
- Нажмите `Cmd + Option + C`
- Или: Разработка → Показать веб-инспектор

**Chrome:**
- Нажмите `Cmd + Option + J`

### 2. Перейдите на вкладку "Console"

### 3. Обновите страницу (Cmd + R)

### 4. Посмотрите ошибки (красным цветом)

Скорее всего увидите ошибку типа:
- "Cannot find module..."
- "Unexpected token..."
- "Router error..."

## 🎯 Альтернатива: Проверка через curl

Проверьте что все assets загружаются:

```bash
curl -I http://localhost/assets/index-BC3igc5s.js
curl -I http://localhost/assets/index-LaswbZWG.css
```

Оба должны вернуть `HTTP/1.1 200 OK`

## 🔧 Временное решение: Локальный dev-сервер

Пока Docker frontend отлаживается, запустите локально:

```bash
cd ~/saa-risk-analyzer/frontend
npm install
npm run dev
```

Откроется на: http://localhost:5173

(API уже работает на порту 8083, так что все запросы будут проксироваться)

