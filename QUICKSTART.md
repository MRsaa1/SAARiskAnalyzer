# 🚀 SAA Risk Analyzer — Быстрый старт

## ✅ Что создано

### Backend (Go)
- ✅ Полная структура проекта с Go 1.22+
- ✅ Математический движок: VaR, CVaR, корреляции, PCA, стресс-тесты, бэктестинг
- ✅ Аутентификация: JWT + RBAC + Argon2id
- ✅ Jobs система с SSE streaming прогресса
- ✅ PostgreSQL с GORM
- ✅ Unit-тесты для математики

### Frontend (React + TypeScript)
- ✅ React 18 + Vite + TypeScript
- ✅ Zustand для state management
- ✅ Tailwind CSS + тема ReserveOne (золотой акцент #caa76a)
- ✅ Страницы: Login, Dashboard
- ✅ Компоненты: MetricCard, RiskItem

### Infrastructure
- ✅ Docker Compose (PostgreSQL, API, Web, Caddy)
- ✅ Caddy reverse proxy
- ✅ Makefile с командами
- ✅ Демо-данные (CSV)
- ✅ Seed скрипты

## 🏃 Запуск за 3 шага

### 1. Подготовка окружения

```bash
cd saa-risk-analyzer

# Скопируйте переменные окружения
cp .env.example .env

# ВАЖНО: Измените JWT_SECRET в .env на случайную строку!
```

### 2. Запуск через Docker

```bash
# Сборка и запуск всех сервисов
make build
make up

# Или одной командой
docker-compose up -d --build
```

Подождите ~30 секунд пока все сервисы запустятся.

### 3. Загрузка демо-данных (опционально)

```bash
# Дождитесь готовности API, затем:
make seed
```

## 🌐 Доступ к платформе

- **Frontend**: http://localhost
- **API Health**: http://localhost/health
- **API Direct**: http://localhost:8083

### Логин по умолчанию:
- Email: `admin@example.com`
- Password: `Admin123456!`

## 📋 Полезные команды

```bash
# Просмотр логов
make logs-api      # Backend логи
make logs-web      # Frontend логи

# Остановка
make down

# Пересборка
make rebuild

# Тесты
make test-backend

# Очистка
make clean
```

## 🐛 Если что-то не работает

### Проблема: Backend не запускается

**Решение:** Проверьте, что PostgreSQL запущен и доступен:

```bash
docker-compose ps
docker-compose logs db
```

### Проблема: Frontend показывает ошибку подключения

**Решение:** Убедитесь, что backend запущен и отвечает:

```bash
curl http://localhost:8083/health
```

Должен вернуть: `{"status":"ok",...}`

### Проблема: Не могу залогиниться

**Решение:** Проверьте логи API и убедитесь, что admin пользователь создан:

```bash
docker-compose logs api | grep "Admin user created"
```

## 🛠 Разработка локально (без Docker)

### Backend

```bash
cd backend

# Установка Go зависимостей (требуется Go 1.22+)
go mod download

# Запуск (требуется PostgreSQL на localhost:5432)
go run cmd/api/main.go
```

### Frontend

```bash
cd frontend

# Установка npm зависимостей (требуется Node 20+)
npm install

# Запуск dev сервера
npm run dev
```

Откройте http://localhost:3000

## 📖 Следующие шаги

1. **Загрузите больше данных**: Добавьте исторические цены в `data/prices.csv`
2. **Создайте портфель**: Импортируйте позиции через API
3. **Запустите расчёты**: Используйте endpoints для VaR, CVaR, PCA
4. **Настройте production**: Раскомментируйте домен в `Caddyfile`

## 🏗 Архитектура

```
saa-risk-analyzer/
├── backend/              # Go API
│   ├── cmd/api/         # Entry point
│   ├── internal/
│   │   ├── math/        # ⭐ Risk engine (VaR, CVaR, PCA...)
│   │   ├── auth/        # JWT, RBAC, Argon2id
│   │   ├── jobs/        # Async jobs + SSE
│   │   └── handlers/    # REST API
│   └── tests/           # Unit tests
├── frontend/            # React SPA
│   ├── src/
│   │   ├── pages/       # Login, Dashboard
│   │   ├── store/       # Zustand stores
│   │   └── styles/      # Theme ReserveOne
│   └── Dockerfile
├── data/                # Demo CSV
├── docker-compose.yml   # Orchestration
└── Makefile             # Commands
```

## 🎨 Кастомизация темы

Цвета находятся в `frontend/src/styles/theme.css`:

```css
:root {
  --bg: #0d0e10;       /* Фон */
  --accent: #caa76a;   /* Золотой акцент ReserveOne */
  --card: #15171b;     /* Карточки */
  /* ... */
}
```

Измените эти значения для адаптации под ваш бренд.

## 📞 Поддержка

- Email: support@reserveone.com
- Telegram: @ReserveOne

---

**Подготовлено ReserveOne** 🏆
