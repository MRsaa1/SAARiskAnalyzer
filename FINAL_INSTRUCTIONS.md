# 🎯 SAA Risk Analyzer — Финальные инструкции

## ⚠️ ВАЖНО: Порты изменены!

Ваши порты 8083, 8085, 8080, 5001 защищены!

### Новые порты:
- API Backend: **8084** (было 8083)
- Frontend Web: **3001** (было 3000)
- Caddy Proxy: **8088** (было 80)
- PostgreSQL: **5433** (было 5432)

---

## 🚀 Запуск

### 1. Перезапустите Docker Desktop
- Откройте приложение Docker Desktop
- Дождитесь иконки кита в menu bar
- Убедитесь что он запущен

### 2. Запустите проект

```bash
cd ~/saa-risk-analyzer
docker compose up -d --build
```

### 3. Откройте в браузере

**Главная ссылка:**
```
http://localhost:8088
```

**Или напрямую:**
- Dashboard: http://localhost:8088/
- Импорт данных: http://localhost:8088/import
- API Health: http://localhost:8084/health

---

## 📂 Страница импорта данных

На http://localhost:8088/import доступно:

### ✍️ Ручной ввод (Manual Input)
- Поля для ввода символа, количества, цены
- Кнопка "Добавить позицию"
- Кнопка "Создать портфель"

### 📂 CSV Import
- Загрузка positions.csv
- Загрузка prices.csv
- Автоматический импорт

---

## 🔄 Что убрано

✅ Убрана система логирования (JWT, RBAC)
✅ Убран текст "Подготовлено ReserveOne 🏆"
✅ Открытый доступ без паролей

---

## 🛠 Управление

```bash
# Просмотр статуса
docker compose ps

# Логи
docker compose logs -f api
docker compose logs -f web

# Остановить
docker compose down

# Перезапустить
docker compose restart
```

---

## 📊 API Endpoints

```bash
# Dashboard метрики
curl http://localhost:8084/api/dashboard

# Создать портфель
curl -X POST http://localhost:8084/api/portfolios \
  -H "Content-Type: application/json" \
  -d '{"name":"Test Portfolio"}'

# Получить портфели
curl http://localhost:8084/api/portfolios
```

---

## ✅ Готово!

Все порты изменены, конфликтов больше нет.

После перезапуска Docker Desktop выполните:

```bash
cd ~/saa-risk-analyzer
docker compose up -d
```

И откройте: **http://localhost:8088**
