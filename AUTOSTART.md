# 🚀 Автозапуск SAA Risk Analyzer

## ✅ Что настроено

Приложение настроено для автоматического запуска после перезагрузки macOS через систему **launchd**.

### Работающие сервисы:
- **Backend API**: http://localhost:8084
- **Frontend**: http://localhost:3001
- **Health Check**: http://localhost:8084/health

### Файлы автозапуска:
- `~/Library/LaunchAgents/com.saa.backend.plist` - Backend сервис
- `~/Library/LaunchAgents/com.saa.frontend.plist` - Frontend сервис

## 📋 Управление сервисами

Используйте скрипт `manage-services.sh` для управления:

```bash
# Показать статус сервисов
./manage-services.sh status

# Запустить сервисы
./manage-services.sh start

# Остановить сервисы
./manage-services.sh stop

# Перезапустить сервисы
./manage-services.sh restart

# Показать логи
./manage-services.sh logs

# Установить автозапуск (уже установлено)
./manage-services.sh install

# Удалить автозапуск
./manage-services.sh uninstall
```

## 🔧 Ручное управление через launchctl

```bash
# Запустить backend
launchctl start com.saa.backend

# Остановить backend
launchctl stop com.saa.backend

# Запустить frontend
launchctl start com.saa.frontend

# Остановить frontend
launchctl stop com.saa.frontend

# Проверить статус
launchctl list | grep com.saa
```

## 📊 Логи

Логи сохраняются в `/tmp/`:
- Backend: `/tmp/saa-backend.log` и `/tmp/saa-backend.error.log`
- Frontend: `/tmp/saa-frontend.log`

Просмотр логов в реальном времени:
```bash
# Backend логи
tail -f /tmp/saa-backend.error.log

# Frontend логи  
tail -f /tmp/saa-frontend.log
```

## 🔄 Автоматический перезапуск

Сервисы настроены с параметром `KeepAlive`, что означает:
- Автоматический запуск при загрузке системы
- Автоматический перезапуск при падении процесса
- Работа в фоновом режиме

## 🛠 Требования

- PostgreSQL 15 (должен быть запущен через brew services)
- Go (для backend)
- Node.js & npm (для frontend)

Проверка PostgreSQL:
```bash
brew services list | grep postgresql
```

Если PostgreSQL не запущен:
```bash
brew services start postgresql@15
```

## 🗄️ База данных

- **Пользователь**: postgres
- **База данных**: risk_db
- **Порт**: 5432
- **Хост**: localhost

## 🌐 Доступ к приложению

После запуска сервисов приложение доступно по адресу:
- **Frontend**: http://localhost:3001
- **API**: http://localhost:8084
- **Health**: http://localhost:8084/health

## 🔥 Устранение проблем

### Сервис не запускается

1. Проверьте логи:
```bash
./manage-services.sh logs
```

2. Проверьте, свободны ли порты:
```bash
lsof -i :8084 -i :3001
```

3. Убейте процессы на портах:
```bash
lsof -ti :8084 | xargs kill -9
lsof -ti :3001 | xargs kill -9
```

4. Перезапустите сервисы:
```bash
./manage-services.sh restart
```

### PostgreSQL не работает

```bash
# Проверить статус
brew services list | grep postgresql

# Запустить
brew services start postgresql@15

# Перезапустить
brew services restart postgresql@15
```

### Пересоздать автозапуск

```bash
# Удалить текущий
./manage-services.sh uninstall

# Установить заново
./manage-services.sh install
```

## 📝 Примечания

- Сервисы запускаются автоматически при входе пользователя в систему
- Изменения в коде требуют перезапуска сервисов
- Для production рекомендуется использовать собранные бинарники вместо `go run`



